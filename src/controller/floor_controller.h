/**
 * This controller acts as an event listener as it listens
 * for changes in the state of the physical elevator and
 * updates the model accordingly.
 */

#ifndef HEIS_EVENT_CONTROLLER_H
#define HEIS_EVENT_CONTROLLER_H

#endif //HEIS_EVENT_CONTROLLER_H

/**
 * Listen for events and updates the model
 */
void update_floor();
